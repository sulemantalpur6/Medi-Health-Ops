spring:
  application:
    name: api-gateway
  config:
    import: configserver:http://localhost:8888
    fail-fast: true
    retry:
      max-attempts: 5
      initial-interval: 1000
      multiplier: 1.5
  cloud:
    config:
      profile: local